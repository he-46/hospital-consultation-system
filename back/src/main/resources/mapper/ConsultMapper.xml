<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.back.mapper.ConsultMapper">

    <select id="selectConsultPage" resultType="org.example.back.entity.Consult">
        SELECT
            c.*,
            d.name AS doctorName,
            d.title AS doctorTitle,
            h.name AS hospitalName
        FROM t_consult c
        LEFT JOIN t_doctor d ON c.doctor_id = d.id
        LEFT JOIN t_hospital h ON d.hospital_id = h.id
        WHERE c.user_id = #{userId}
        <if test="status != null">
            AND c.status = #{status}
        </if>
        ORDER BY c.create_time DESC
    </select>

    <select id="selectConsultById" resultType="org.example.back.entity.Consult">
        SELECT
            c.*,
            d.name AS doctorName,
            d.title AS doctorTitle,
            h.name AS hospitalName
        FROM t_consult c
        LEFT JOIN t_doctor d ON c.doctor_id = d.id
        LEFT JOIN t_hospital h ON d.hospital_id = h.id
        WHERE c.id = #{id}
    </select>

</mapper>
